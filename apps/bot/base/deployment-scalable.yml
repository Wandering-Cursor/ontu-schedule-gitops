---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ontu-schedule-bot-scalable
spec:
  replicas: 0
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: ontu-schedule-bot
  template:
    metadata:
      labels:
        app: ontu-schedule-bot
    spec:
      containers:
      - name: ontu-schedule-bot-scalable
        imagePullPolicy: Always
        image: ghcr.io/wandering-cursor/ontu-schedule-bot:latest
        env:
        - name: RUN_PERIODIC_JOBS
          value: "False"
        - name: PORT
          value: "80"
