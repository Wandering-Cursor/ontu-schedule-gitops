apiVersion: v1
kind: Service
metadata:
  name: ontu-schedule-bot-admin
spec:
  type: ClusterIP
  selector:
    app: ontu-schedule-bot-admin
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 443
